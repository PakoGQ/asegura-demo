# ── Fuera del repositorio público ────────────────────────────────────────────
# Estos archivos se quedan en la máquina. Contienen el modelo de negocio, los
# precios, quién es el cliente beta y de qué proyecto se portó el código.
CLAUDE.md
docs/
tools/

# Demo viejo del CRM: quedó desfasado tras el cambio de rumbo.
demo/

# Generado por tools/armar_sql.py a partir de db/01…07. No aporta nada al
# repositorio que no esté ya en los archivos numerados, y duplicaría en un
# archivo más las menciones al proyecto de referencia que hay en sus comentarios.
db/TODO_EN_UNO.sql

# ── Nunca ──
.DS_Store
*.log
.env
.env.*
*service_role*
*.dump
*.sql.bak
respaldos/
